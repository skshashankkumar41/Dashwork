import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "DashBot",
  initialMessages: [createChatBotMessage(`Check Intents by typing Utterances`)],
};

export default config;
